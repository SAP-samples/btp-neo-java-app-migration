<?xml version="1.0" encoding="UTF-8"?>
<!--
  SAP BTP Cloud Foundry - TomEE Application POM Template
  
  Usage: Copy this template for applications using EJB/JavaEE features.
  1. Update groupId, artifactId, and version
  2. Adjust SDK versions if needed
  3. Add application-specific dependencies
  4. Remember to set TARGET_RUNTIME: tomee in mtad.yaml
-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 
                             http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!-- TODO: Update project coordinates -->
    <groupId>com.example</groupId>
    <artifactId>my-cf-ejb-app</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <packaging>war</packaging>

    <properties>
        <!-- Compiler settings -->
        <java.version>25</java.version>
        <maven.compiler.source>${java.version}</maven.compiler.source>
        <maven.compiler.target>${java.version}</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <!-- SAP BTP SDK versions — resolve via the lookup script
             (see sdk-replacement Step 3); do NOT ship literal placeholders.
             Note: Using cf-tomee-bom instead of cf-tomcat-bom. -->
        <cf-tomee-bom-version>RESOLVED_CF_TOMEE_BOM_VERSION</cf-tomee-bom-version>
        <sdk-modules-bom-version>RESOLVED_SDK_MODULES_BOM_VERSION</sdk-modules-bom-version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- CF TomEE BOM - provides full Jakarta EE APIs -->
            <dependency>
                <groupId>com.sap.cloud.sjb.cf</groupId>
                <artifactId>cf-tomee-bom</artifactId>
                <version>${cf-tomee-bom-version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <!-- SAP Cloud SDK BOM -->
            <dependency>
                <groupId>com.sap.cloud.sdk</groupId>
                <artifactId>sdk-modules-bom</artifactId>
                <version>${sdk-modules-bom-version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <!-- SAP Cloud SDK for Cloud Foundry -->
        <dependency>
            <groupId>com.sap.cloud.sdk.cloudplatform</groupId>
            <artifactId>scp-cf</artifactId>
        </dependency>

        <!-- Jakarta EE 10 API (provided by TomEE) -->
        <dependency>
            <groupId>jakarta.platform</groupId>
            <artifactId>jakarta.jakartaee-api</artifactId>
            <version>10.0.0</version>
            <scope>provided</scope>
        </dependency>

        <!-- SLF4J Logging -->
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
        </dependency>
        <dependency>
            <groupId>ch.qos.logback</groupId>
            <artifactId>logback-classic</artifactId>
        </dependency>

        <!-- ======================= -->
        <!-- Optional dependencies   -->
        <!-- ======================= -->

        <!-- XSUAA Security (uncomment if using authentication) -->
        <!--
        <dependency>
            <groupId>com.sap.cloud.security</groupId>
            <artifactId>java-security</artifactId>
        </dependency>
        -->

        <!-- JSON Processing (Jackson) -->
        <!--
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
        </dependency>
        -->

        <!-- ======================= -->
        <!-- Test dependencies       -->
        <!-- ======================= -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>5.10.0</version>
            <scope>test</scope>
        </dependency>
        
        <!-- TomEE Embedded for testing -->
        <dependency>
            <groupId>org.apache.tomee</groupId>
            <artifactId>openejb-junit5</artifactId>
            <version>9.1.2</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <finalName>${project.artifactId}</finalName>
        <plugins>
            <!-- Compiler Plugin -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.11.0</version>
                <configuration>
                    <source>${java.version}</source>
                    <target>${java.version}</target>
                </configuration>
            </plugin>

            <!-- WAR Plugin

                 warName is deliberately set to ${project.artifactId} (not ROOT).
                 Consequences the migration must keep aligned:
                   - mtad.yaml's `path:` MUST be `target/<artifactId>.war`,
                     written literally (cf deploy does not evaluate Maven
                     property expressions).
                   - TomEE serves the WAR at /<artifactId>, not /.
                     Integration tests and approuter destinations must
                     include the /<artifactId> prefix in their URLs.

                 See `mta-descriptor` → "WAR filename rule" for the matching
                 descriptor-side guidance. -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-war-plugin</artifactId>
                <version>3.4.0</version>
                <configuration>
                    <warName>${project.artifactId}</warName>
                    <failOnMissingWebXml>false</failOnMissingWebXml>
                </configuration>
            </plugin>

            <!-- Surefire Plugin for tests -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.1.2</version>
            </plugin>
        </plugins>
    </build>
</project>
