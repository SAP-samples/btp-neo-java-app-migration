<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <name>Password Storage Integration Test</name>
    <groupId>com.sap.cloud.sample</groupId>
    <artifactId>pass-store-integration-test</artifactId>
    <version>1.0.0</version>

    <properties>
        <sap.cloud.jdk.version>25</sap.cloud.jdk.version>
        <slf4j.version>1.7.30</slf4j.version>

        <hamcrest-library.version>1.3</hamcrest-library.version>
        <junit.version>4.10</junit.version>
        <test.framework.version>2.88.0</test.framework.version>
        <easymock.version>3.1</easymock.version>
        <httpunit.version>1.7.3</httpunit.version>
        <nimbus-jose-jwt.version>9.40</nimbus-jose-jwt.version>
        <bouncycastle.version>1.78.1</bouncycastle.version>
        <jackson.version>2.17.2</jackson.version>

        <maven-surefire.version>3.5.0</maven-surefire.version>
        <maven-failsafe.version>3.5.0</maven-failsafe.version>
        <maven-enforcer.version>1.0.1</maven-enforcer.version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.hamcrest</groupId>
            <artifactId>hamcrest-library</artifactId>
            <version>${hamcrest-library.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
            <version>${junit.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.httpunit</groupId>
            <artifactId>httpunit</artifactId>
            <version>${httpunit.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>com.sap.jpaas.test.framework</groupId>
            <artifactId>com.sap.jpaas.test.framework.util</artifactId>
            <version>${test.framework.version}</version>
            <scope>test</scope>
        </dependency>
        <!-- Seeding the credential store before the tests run: JOSE (JWE) payload
             construction + PEM key parsing + JSON. See PassStoreSeeder. -->
        <dependency>
            <groupId>com.nimbusds</groupId>
            <artifactId>nimbus-jose-jwt</artifactId>
            <version>${nimbus-jose-jwt.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.bouncycastle</groupId>
            <artifactId>bcpkix-jdk18on</artifactId>
            <version>${bouncycastle.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
            <version>${jackson.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>${maven-surefire.version}</version>
                <configuration>
                    <includes>
                        <include>**/*Test.java</include>
                    </includes>
                    <excludes>
                        <exclude>**/*IntegrationTest.java</exclude>
                    </excludes>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <profile>
            <id>integration-tests</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-failsafe-plugin</artifactId>
                        <version>${maven-failsafe.version}</version>
                        <executions>
                            <execution>
                                <id>integration-tests</id>
                                <goals>
                                    <goal>integration-test</goal>
                                    <goal>verify</goal>
                                </goals>
                                <configuration>
                                    <includes>
                                        <include>**/*IntegrationTest.java</include>
                                    </includes>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>

                    <plugin>
                        <artifactId>maven-enforcer-plugin</artifactId>
                        <version>${maven-enforcer.version}</version>
                        <executions>
                            <execution>
                                <id>enforce-maven</id>
                                <goals>
                                    <goal>enforce</goal>
                                </goals>
                                <configuration>
                                    <rules>
                                        <requireProperty>
                                            <property>app.url</property>
                                            <message>(-Dapp.url=...) is missing, but required!</message>
                                        </requireProperty>
                                    </rules>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
